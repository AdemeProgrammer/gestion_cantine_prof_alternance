{% extends 'base.html.twig' %}

{% block title %}Modifier une promotion{% endblock %}

{% block body %}
    <h1>Modifier une promotion</h1>

    {{ include('promo/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_promo_index') }}">back to list</a>

    {{ include('promo/_delete_form.html.twig') }}
{% endblock %}
