{% extends 'base.html.twig' %}
{% import "_macros/breadcumb.html.twig" as _breadcumb %}

{% block title %}Détails de la promotion{% endblock %}
{% block titre_page %}Détails de la promotion{% endblock %}
{% block action_page %}
    <a href="{{ path('app_promo_edit', {'id': promo.id}) }}" class="btn btn-primary">Modifier</a>
{% endblock %}

{% block Breadcrumb %}
    {{ _breadcumb.etape("Promotion") }}
    {{ _breadcumb.etape("Détails") }}
{% endblock %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promo.id }}</td>
            </tr>
            <tr>
                <th>Annee_debut</th>
                <td>{{ promo.anneeDebut }}</td>
            </tr>
            <tr>
                <th>Annee_fin</th>
                <td>{{ promo.anneeFin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_promo_index') }}">Revenir à la liste</a>

    {{ include('promo/_delete_form.html.twig') }}
{% endblock %}
